<?php

namespace App\Tasks;

class BubbleSort
{
    function sort($array): array
    {
        if (count($array) < 2) {
            return $array;
        }

        for ($i = 0; $i < count($array)-1; $i++) {

        }

        return $array;
    }
}